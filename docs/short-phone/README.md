# Short-phone Pick up / Pass shots

Real Playwright screenshots from local Vite (PR branch, not GitHub Pages / main).

Binary PNGs were captured on the executor box but `gh` was not authenticated, so they could not be committed from MCP (`create_or_update_file` is text-only).

**Box paths for Code Lead to attach**

- `/workspace/hearts-pr2/docs/short-phone/euchre-pickup-375x667.png`
- `/workspace/hearts-pr2/docs/short-phone/euchre-pickup-320x568.png`
- `/workspace/hearts-pr2/docs/short-phone/euchre-pickup-390x844.png`

Copies: `/workspace/docs/short-phone/`

HUD CSS follow-up: `fde84226` on this branch. CSS-pass start: `5a2d9caa`. No main push.
